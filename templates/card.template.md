# <Noun name>  (live)

<!-- Copy to map/cards/<noun>.md. The heading must end with exactly one tag in parentheses:
     (live), (leftover), or (ghost). One card per file. Cite source; never copy the source in. -->

**Source:** <where this noun really lives — table id / field ids, or a file path — so a reader lands on it in one hop. A record count belongs here for a database card; omit it on a folder/repo card.>
**What it is:** <one or two sentences: what this noun is, why it is shaped this way, and why it matters — the business meaning / why a change here is consequential (a rarely-run rollback path is still critical)>
**Shape:** <the parts that matter, not all of them — the fields, nodes, or files a change touches>
**Hits:** <what else moves if you change this noun; name the specific downstream nouns>
**Does not hit:** <the obvious neighbour a reader will assume is downstream and is not — this line is the point. Bound it: name the surfaces you checked and as-of when; never a bare absence.>
**Before you change this:** <optional, for humans — safe to edit: ... · needs review: ... · never change alone (open first): cards/...>
