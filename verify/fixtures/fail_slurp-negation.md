# Fixture: MUST FAIL (slurp carrying a mid-line negation to fool the guard)

The imperative still loads the whole source; the parenthetical negation must not
whitewash it. A negation only counts when it LEADS the line.

Load every file in the objects folder (do not skip any) before answering.
