# Fixture: MUST FAIL (tells the reader to slurp)

Walk instructions that break the one rule by loading the whole source.

Load every file in the objects folder into the project before you start.
Read the entire base into context, then answer.
